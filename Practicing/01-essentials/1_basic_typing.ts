// Declare a variable `city` of type `string`
// and assign it the value `"Moscow"`.
// Then declare a variable `population`
// that can be either a `number` or a `string`,
// and assign it the value `12_000_000`.

let city: string;
city = 'Moscow';

let population: string | number;
population = '12_000_000';
