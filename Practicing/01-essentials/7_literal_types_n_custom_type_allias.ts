// Define a type:

// type Permission = 'read' | 'write' | 'delete';

// Then create a variable `userPermission` of this type.
// Try assigning `'execute'` to it and observe the error.

type Permission = 'read' | 'write' | 'delete';

// const userPermission: Permission = 'execute'
